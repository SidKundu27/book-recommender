import React from 'react';
import './Spinner.css';

const Spinner = () => {
  return (
    <div className="custom-spinner">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;